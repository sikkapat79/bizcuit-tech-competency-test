{
  "name": "bizcuit-tech-competency-test",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "build": "docker-compose up -d --build",
    "start": "docker-compose up -d",
    "destroy": "docker-compose down --remove-orphans --rmi all",
    "mock:beer": "docker-compose -f docker-compose.fetch.yml run --rm fetcher node /app/fetch.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC"
}
