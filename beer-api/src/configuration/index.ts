export default interface IConfiguariton {
  // Global environment
  NOD_ENV: string;

  // Database Configuraion
  DB_USER: string;
  DB_HOST: string;
  DB_PORT: number;
  DB_PASSWORD: string;
  DB_DATABASE: string;
}
